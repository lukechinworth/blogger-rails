<%= form_for [@post, @comment], remote: true do |f| %>
  <div class="form-group">
    <%= f.label :body, "Add your comment" %>
    <%= f.text_area :body, rows: "4", class: "form-control", id: "body" %>
  </div>
  <%= f.submit "Post", class: "btn" %>
<% end %>
