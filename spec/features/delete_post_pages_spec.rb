require 'rails_helper'

describe "the delete a post process" do
  # commented this test out because the selenium driver takes forever to actually open up
  # firefox and run the test

  # it "deletes a post", js: true do
  #   Post.create(title: "test", body: "test body")
  #
  #   visit posts_path
  #   click_on 'test'
  #   accept_confirm do
  #     click_on 'Delete'
  #   end
  #   expect(page).to have_content 'successfully deleted'
  # end
end
